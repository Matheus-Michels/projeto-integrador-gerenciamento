version: "3.8"
services:
  db:
    image: postgres:15
    container_name: postgres_pi
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: gerenciamento_atividades
    ports:
      - "5432:5432"
